<?xml version="1.0" encoding="utf-8" standalone="yes"?><rss version="2.0" xmlns:atom="http://www.w3.org/2005/Atom"><channel><title>Docker on Buffer Overflow</title><link>/notes/linux/docker/</link><description>Recent content in Docker on Buffer Overflow</description><generator>Hugo -- gohugo.io</generator><language>en</language><lastBuildDate>Sat, 14 Dec 2024 00:00:00 +0000</lastBuildDate><atom:link href="/notes/linux/docker/index.xml" rel="self" type="application/rss+xml"/><item><title>NVIDIA Container Toolkit with Docker CDI</title><link>/notes/linux/docker/nvidia_ctk_docker_cdi/</link><pubDate>Sat, 14 Dec 2024 00:00:00 +0000</pubDate><guid>/notes/linux/docker/nvidia_ctk_docker_cdi/</guid><description>&lt;p>I recently spent an hour (or three) troubleshooting hardware transcoding on my media server. At some point, an update to the NVIDIA Container Toolkit resulted in CUDA libraries not getting passed through correctly to my containerized Plex installation.&lt;/p>
&lt;p>This error appeared in the &lt;code>Plex Media Server.log&lt;/code> file every time I attempted to play a file that needed transcoding:&lt;/p>
&lt;pre tabindex="0">&lt;code>Dec 15, 2024 03:45:14.374 [140307690093368] ERROR - [Req#105/Transcode] [FFMPEG] - Cannot load libcuda.so.1
Dec 15, 2024 03:45:14.374 [140307690093368] ERROR - [Req#105/Transcode] [FFMPEG] - Could not dynamically load CUDA
&lt;/code>&lt;/pre>&lt;p>Running &lt;code>nvidia-smi&lt;/code> inside the container also presented the following error:&lt;/p></description><content>&lt;p>I recently spent an hour (or three) troubleshooting hardware transcoding on my media server. At some point, an update to the NVIDIA Container Toolkit resulted in CUDA libraries not getting passed through correctly to my containerized Plex installation.&lt;/p>
&lt;p>This error appeared in the &lt;code>Plex Media Server.log&lt;/code> file every time I attempted to play a file that needed transcoding:&lt;/p>
&lt;pre tabindex="0">&lt;code>Dec 15, 2024 03:45:14.374 [140307690093368] ERROR - [Req#105/Transcode] [FFMPEG] - Cannot load libcuda.so.1
Dec 15, 2024 03:45:14.374 [140307690093368] ERROR - [Req#105/Transcode] [FFMPEG] - Could not dynamically load CUDA
&lt;/code>&lt;/pre>&lt;p>Running &lt;code>nvidia-smi&lt;/code> inside the container also presented the following error:&lt;/p>
&lt;pre tabindex="0">&lt;code>NVIDIA-SMI couldn&amp;#39;t find libnvidia-ml.so library in your system. Please make sure that the NVIDIA Display Driver is properly installed and present in your system.
Please also try adding directory that contains libnvidia-ml.so to your system PATH.
&lt;/code>&lt;/pre>&lt;p>Checking the library directory &lt;code>/usr/lib/x86_64-linux-gnu/&lt;/code> in the container revealed the missing piece:&lt;/p>
&lt;pre tabindex="0">&lt;code>libcuda.so -&amp;gt; libcuda.so.1 (MISSING)
libcuda.so.535.183.01
libnvcuvid.so.535.183.01
libnvidia-allocator.so.535.183.01
libnvidia-cfg.so.535.183.01
libnvidia-eglcore.so.535.183.01
libnvidia-egl-gbm.so.1.1.0
libnvidia-egl-wayland.so.1.1.10
libnvidia-glcore.so.535.183.01
libnvidia-glsi.so.535.183.01
libnvidia-glvkspirv.so.535.183.01
libnvidia-ml.so.535.183.01
libnvidia-pkcs11-openssl3.so.535.183.01
libnvidia-ptxjitcompiler.so.535.183.01
libnvidia-rtcore.so.535.183.01
libnvidia-tls.so.535.183.01
&lt;/code>&lt;/pre>&lt;p>Note the absence of default libraries - in this case, components like &lt;code>libnvidia-ml.so.565.57.01&lt;/code> which should have a symlink called &lt;code>libnvidia-ml.so&lt;/code> which makes it the default version. This also explains the error above for &lt;code>libcuda.so.1&lt;/code>, which should have been reachable via &lt;code>libcuda.so&lt;/code> if the link weren&amp;rsquo;t missing.&lt;/p>
&lt;p>I saw that the &lt;code>nvidia-container-toolkit&lt;/code> had undergone quite a few updates since I first set it all up, so I tried updating the NVIDIA drivers on my system from &lt;strong>535.183.01&lt;/strong> to &lt;strong>565.57.01&lt;/strong> to see if installing newer drivers somehow forced the CTK to relink everything. That didn&amp;rsquo;t help, and instead resulted in more libraries for the new version that still weren&amp;rsquo;t linking to defaults.&lt;/p>
&lt;p>I manually ran the &lt;code>ldconfig&lt;/code> linker inside the container and observed that it correctly generated the symlinks. Plex could now see the CUDA library and &lt;code>nvidia-smi&lt;/code> could report the GPU, but this was only a temporary fix and wouldn&amp;rsquo;t survive a container rebuild.&lt;/p>
&lt;p>Research led me to a &lt;a href="https://github.com/NVIDIA/nvidia-container-toolkit/issues/128">GitHub issue on the &lt;code>nvidia-container-toolkit&lt;/code> project&lt;/a> related to &lt;a href="https://github.com/NVIDIA/nvidia-container-toolkit/blob/main/cmd/nvidia-ctk/README.md">CDI (container device interface) functionality&lt;/a>. CDI support seems to be a relatively new feature of the CTK which manages presenting the correct driver libraries through the container runtime. I didn&amp;rsquo;t have any CDI configurations present on my system, so I followed the docs to generate one.&lt;/p>
&lt;pre tabindex="0">&lt;code>nvidia-ctk cdi generate --output=/etc/cdi/nvidia.yaml
nvidia-ctk runtime configure --runtime=docker --cdi.enabled
systemctl restart docker
&lt;/code>&lt;/pre>&lt;p>I also had to the add the device &lt;code>nvidia.com/gpu=all&lt;/code> to the Plex service in my &lt;code>docker-compose.yaml&lt;/code> file so the NVIDIA container runtime knows to use the CDI configuration.&lt;/p>
&lt;pre tabindex="0">&lt;code> plex:
image: ghcr.io/linuxserver/plex
runtime: nvidia
environment:
- NVIDIA_VISIBLE_DEVICES=all
devices:
- nvidia.com/gpu=all
&lt;/code>&lt;/pre>&lt;p>Once I rebuilt the Plex container with the CDI configuration, all of the NVIDIA libraries in the container were correctly linked with default &lt;code>.so.&lt;/code> versions and hardware transcoding worked as expected.&lt;/p></content></item></channel></rss>